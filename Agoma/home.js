// Declarations
const nameInput = document.getElementById("fullnameInput").value
const aliasInput = document.getElementById("aliasInput").value
const submitBtn = document.querySelector(".submitBtn")



submitBtn.addEventListener( "click" , (e)=>{
    e.preventDefault();

} )